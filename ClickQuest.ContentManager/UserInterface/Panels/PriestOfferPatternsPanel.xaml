<UserControl x:Class="ClickQuest.ContentManager.UserInterface.Panels.PriestOfferPatternsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Background="White"
             d:DesignHeight="940" d:DesignWidth="1920">
	<Grid x:Name="MainGrid">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="2*" />
			<ColumnDefinition Width="2*" />
		</Grid.ColumnDefinitions>

		<StackPanel x:Name="SelectionPanel" Grid.Column="0">
			<TextBlock Text="Choose object by name" Margin="20,100,20,5" HorizontalAlignment="Center" FontSize="16" />
			<ComboBox x:Name="ContentSelectionBox" Margin="20,20,20,5" SelectionChanged="ContentSelectionBox_SelectionChanged" />
			<Button x:Name="AddNewObjectButton" Content="Add new" Margin="20,50,20,5" Style="{StaticResource MaterialDesignFlatDarkBgButton}" Click="AddNewObjectButton_Click" />
			<Button x:Name="DeleteObjectButton" Content="Delete" Margin="20,50,20,5" Style="{StaticResource MaterialDesignFlatDarkBgButton}" Background="Red" Visibility="Hidden" Click="DeleteObjectButton_Click" />
		</StackPanel>

		<StackPanel x:Name="Separator" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Right">
			<Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Foreground="Black" Width="2" />
		</StackPanel>
	</Grid>
</UserControl>